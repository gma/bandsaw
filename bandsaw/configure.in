AC_INIT(src/bandsaw.py)

dnl AM_CONFIG_HEADER(config.h)
AM_INIT_AUTOMAKE(bandsaw, 0.1)

AM_MAINTAINER_MODE

AC_PATH_PROG(GCONFTOOL, gconftool-2)
AM_GCONF_SOURCE_2

AM_PATH_PYTHON(2.3)

PKG_CHECK_MODULES(PYGTK, pygtk-2.0)
AC_SUBST(PYGTK_CFLAGS)
AC_SUBST(PYGTK_LIBS)

AC_OUTPUT(Makefile)