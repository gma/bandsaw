TODO
----

  - default config options (i.e. be robust to reading missing ones).

  - ensure alert suppression params read from and written to gconf.

  - ensure that spin button changes affect gconf when value is typed.

  - fix auto scrolling performance issues.

  - only modify general config params when moving out of widget.

  - improve auto scrolling performance.

$Id$
